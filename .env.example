# SciX unified environment variables (example)
#
# Copy to .env and fill in real values:
#   cp .env.example .env
#
# IMPORTANT:
# - Do NOT commit .env (it's already gitignored)
# - GitHub webhooks generally require a PUBLIC HTTPS endpoint.

# -----------------------------
# sciland service
# -----------------------------
PORT=8000
APP_ENV=development

# GitHub
GITHUB_TOKEN=ghp_xxx
# Optional: requester flow token for /api/v1/challenges/request
GITHUB_TOKEN2=
GITHUB_ORG=scix-lab
GITHUB_API_BASE=https://api.github.com

# Public API base URL (HTTPS) that GitHub can reach.
# sciland uses this to auto-create GitHub repo webhooks pointing to:
#   ${API_PUBLIC_BASE_URL}/api/v1/webhooks/github
# Example: https://api.yourdomain.com
API_PUBLIC_BASE_URL=

# GitHub → API webhook verification secret (for /api/v1/webhooks/github)
# Must match the "Secret" you set in GitHub repo webhooks.
GITHUB_WEBHOOK_SECRET=replace-with-webhook-secret

# sciland moderator gate (sciland checks MODERATOR_API_KEY)
MODERATOR_API_KEY=replace-with-a-long-random-secret

# Repo convention
CHALLENGE_REPO_PREFIX=challenge
VERSION_BRANCHES=version/v1,version/v2

# Cache
CACHE_TTL_SECONDS=30
CACHE_FILE=data/webhook_cache.json

# -----------------------------
# SciXbook/api service (Express)
# -----------------------------
# api calls sciland with this key (must equal MODERATOR_API_KEY)
SCILAND_MODERATOR_API_KEY=replace-with-a-long-random-secret

# Optional: internal sciland→api webhook token (if you still use /api/v1/webhooks/sciland)
SCILAND_WEBHOOK_TOKEN=local-webhook-token
